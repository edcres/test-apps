package com.example.coolfunctions.leetcode

fun main() {
    val skratch = Skratch()

    skratch.test(intArrayOf())
}

class Skratch {

    fun test(nums: IntArray): List<Int> {
        val lknklfs = nums.sortedBy { it }.toMutableList()

        val asdfgh = nums.sorted().toMutableList()

        return lknklfs
    }
}
package com.aldreduser.testapp.recyclergridlayout

// I haven't don anything here yet but I'll just have basic syntax for now

private class placeHolderGrid() {

    // recyclerView.layoutManager = GridLayoutManager(applicationContext,2)
}
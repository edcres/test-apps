pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral() { url 'https://jitpack.io' }     // For MPAndroidChart
//        maven { url 'https://jitpack.io' }      // For MPAndroidChart
    }
}
rootProject.name = "Test Charts"
include ':app'
